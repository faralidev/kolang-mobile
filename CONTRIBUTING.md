# مشارکت

از مشارکت شما سپاسگزاریم! راهنمای کوتاه مشارکت در «ویرایشگر کلنگ»:

## شروع

1. مخزن را فورک کنید و یک شاخهٔ جدید بسازید:
   `git checkout -b feature/my-change`
2. تغییرات را اعمال و تست کنید (تست‌های مربوطه را اجرا کنید).
3. یک pull request با توضیح واضح باز کنید.

## قواعد

- **فارسی درست:** متن‌های فارسی با نیم‌فاصله (ZWNJ) و ارقام فارسی صحیح نوشته
  شوند. از ترجمه‌های ماشینی بدون بازبینی استفاده نکنید.
- **یک کدبیس:** تغییری که یک کدبیس را به «وب/نیتیو» دوشاخه کند پذیرفته نمی‌شود.
- **قرارداد خروجی مفسر:** تابع `runKolang` همیشه
  `Promise<{ ok, output, error }>` برمی‌گرداند — آن را تغییر ندهید.
- **کد تمیز:** با `npm run lint` خطاها را بررسی کنید.
- **آزمون‌ها:** برای تغییرات منطقی، تست بنویسید یا تست موجود را به‌روز کنید.

## گزارش باگ

باگ‌ها را در بخش Issues با عنوان فارسی و شرح قدم‌به‌قدم (سیستم‌عامل، پلتفرم هدف،
کد بازتولید) ثبت کنید.